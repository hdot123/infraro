# 自建 runner label，actionlint 内置清单不认识，需显式声明：
# - pve-linux: org 级 Linux runner（droid-review 聚合 job 所在）
# 对齐引擎仓 .github/actionlint.yaml 与消费仓同文件（r38 形态）。
self-hosted-runner:
  labels:
    - self-hosted
    - pve-linux

# e2e validation marker 2026-09-18: droid-review shard+aggregate pipeline check
