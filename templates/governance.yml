name: Governance
on:
  pull_request_target:
    branches: [main]
    paths:
      - '.evolution/**'
      - '.github/workflows/**'
      - 'src/infra_core/engine/**'
      - 'webhook-scripts/**'
      - 'actions/**'

permissions:
  contents: read
  pull-requests: read

jobs:
  governance:
    uses: hdot123/infraro-core/.github/workflows/evolution-governance.yml@v0.18.4
