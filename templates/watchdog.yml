name: Watchdog
on:
  schedule:
    - cron: '30 */2 * * *'  # Every 2 hours at 30 minutes past the hour (avoiding peak times)
  workflow_dispatch:
  workflow_call:
    inputs:
      engine_ref:
        required: false
        type: string
        description: "Engine ref to use"
    secrets:
      DISPATCH_TOKEN:
        required: true
jobs:
  watchdog:
    runs-on: [self-hosted, pve-linux]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Install infraro engine
        run: pip install git+https://github.com/hdot123/infraro-core.git@v0.18.4
      
      - name: Run watchdog monitoring
        run: infraro watchdog --repo ${{ github.repository }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DISPATCH_TOKEN: ${{ secrets.DISPATCH_TOKEN }}
